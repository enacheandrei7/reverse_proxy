apiVersion: v1
kind: Service
metadata:
  name: {{ include "rev_proxy_helm.fullname" . }}
  labels:
    {{- include "rev_proxy_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rev_proxy_helm.selectorLabels" . | nindent 4 }}
